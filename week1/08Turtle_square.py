# draw a square

import turtle as t

length = 100
angle = 60

# t.forward(length)
# t.left(angle)
# t.forward(length)
# t.left(angle)
# t.forward(length)

t.left(angle // 2)
t.forward(length)
t.left(angle)
t.forward(length)
t.left(angle)
t.forward(length)
t.left(angle)
t.forward(length)
t.left(angle)
t.forward(length)
t.left(angle)
t.forward(length)

t.exitonclick()
